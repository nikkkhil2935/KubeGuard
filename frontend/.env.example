# Backend URL for the existing KubeGuard live server
KG_BACKEND_URL=http://localhost:9001

# Optional explicit websocket endpoint
# NEXT_PUBLIC_KG_WS_URL=ws://localhost:9001/ws

# Optional websocket port fallback if NEXT_PUBLIC_KG_WS_URL is not set
NEXT_PUBLIC_KG_WS_PORT=9001
